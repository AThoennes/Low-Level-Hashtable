public interface HashtableInterface
{
	// insert a value into the hashtable
	void insert(int value);
	
	// delete a value in the hastable
	void delete(int value);
	
	//display everything in the hashtable
	void dumpHash();
}
